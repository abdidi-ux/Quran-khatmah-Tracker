{
  "name": "quran-khatmah-circle",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser.mjs",
    "check": "node --check app.js && node --check core.mjs && npm test"
  }
}
